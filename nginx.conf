server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # HTTP 健康检查
    location = /healthz {
        default_type text/plain;
        return 200 'ok';
    }

    # 静态资源与 SPA 回退
    location / {
        try_files $uri $uri/ /index.html;
    }
}
